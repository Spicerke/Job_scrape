# the database — postings, settings, resume text, and the resume PDFs
*.db
*.db-wal
*.db-shm

# secrets
.env

# Your actual resumes. These carry your phone number, personal email, and
# LinkedIn, and this repo is public — keep them out of it. Copy them to the
# Pi with scp instead (see deploy/HOSTING.md), the same way .env gets there.
resumes/
resume.tex
resume.pdf
*-Resume.pdf

__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store
